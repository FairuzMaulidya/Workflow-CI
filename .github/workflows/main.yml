name: CI_MLflow_Project

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
  MLFLOW_TRACKING_USERNAME: ${{ secrets.MLFLOW_TRACKING_USERNAME }}
  MLFLOW_TRACKING_PASSWORD: ${{ secrets.MLFLOW_TRACKING_PASSWORD }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Setup Python
      - name: Set up Python 3.13.5
        uses: actions/setup-python@v4
        with:
          python-version: "3.13.5"

      # 3. List project structure (debug)
      - name: List project structure
        run: ls -R

      # 4. Check environment variables
      - name: Check Env Variables
        shell: bash -l {0}
        run: |
          echo "MLFLOW_TRACKING_URI=$MLFLOW_TRACKING_URI"
          echo "MLFLOW_TRACKING_USERNAME=$MLFLOW_TRACKING_USERNAME"

      # 5. Install dependencies from conda.yaml (inside MLProject folder)
      - name: Install dependencies (conda)
        shell: bash -l {0}
        run: |
          source /usr/share/miniconda/etc/profile.d/conda.sh
          conda env create -f MLProject/conda.yaml || conda env update -f MLProject/conda.yaml --prune
          conda activate mlflow-env
          which python
          python --version

      # 6. Run MLflow project (inside MLProject folder)
      - name: Run MLflow project
        shell: bash -l {0}
        run: |
          source /usr/share/miniconda/etc/profile.d/conda.sh
          conda activate mlflow-env
          cd MLProject
          which mlflow
          mlflow --version
          mlflow run . --env-manager=local

      # 7. Get latest MLflow run_id
      - name: Get latest MLflow run_id
        shell: bash -l {0}
        run: |
          cd MLProject
          RUN_ID=$(ls -td mlruns/0/*/ | head -n 1 | cut -d'/' -f3)
          echo "RUN_ID=$RUN_ID" >> $GITHUB_ENV
          echo "✅ Latest run_id: $RUN_ID"
